#if !defined(BOB_H)
#define BOB_H

#include <array>
#include <string>
#include <algorithm>
#include <cctype>
#include <cstdint>

namespace bob {
    auto hey(const std::string& dialogue) -> std::string;
}  // namespace bob

#endif // BOB_H